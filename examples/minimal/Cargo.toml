[package]
name = "minimal"
version = "0.1.0"
license = "MIT OR Apache-2.0"
edition = "2024"

[dependencies]
leptos = { version = "0.7", features = ["csr"] }
leptos-musaic = { path = "../../crates/musaic", features = ["themes"] }
console_error_panic_hook = "0.1"
